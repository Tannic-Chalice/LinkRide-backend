package com.linkride.backend.enums;

public enum FavoriteType {
    HOME,   // User's home address (max 1 per user)
    WORK,   // User's work address (max 1 per user)
    CUSTOM  // Any user-defined place
}
